// pointer state changes on move
// When background clicked, showInput = true, then false
// When showInput = true, character options display
// When character is found, input is updated
// When character is found, mouse location is compared to character locations